import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "TravelX - Enterprise Travel Agency Management",
  description: "Production-ready SaaS platform for travel agency management. Manage leads, bookings, payments, documents, and reports across multiple agencies.",
  keywords: ["travel agency", "booking management", "travel software", "agency management"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.variable}>
        {children}
      </body>
    </html>
  );
}
